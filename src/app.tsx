import React from "react";
import { Page, PageSection } from "@patternfly/react-core";

export const Application = () => {
    return (
        <Page>
            <PageSection>
                <h1>Dashboard</h1>
            </PageSection>
        </Page>
    );
};
